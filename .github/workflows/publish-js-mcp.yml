name: Publish JavaScript MCP Server

on:
  push:
    tags:
      - "mcp@*"

env:
  BUN_VERSION: "1.3.12"

jobs:
  validate:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v5
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: ${{ env.BUN_VERSION }}
      - uses: actions/cache@v5
        with:
          path: ~/.bun/install/cache
          key: ${{ runner.os }}-bun-${{ hashFiles('**/bun.lock') }}
          restore-keys: ${{ runner.os }}-bun-
      - run: bun install --frozen-lockfile
      - name: Validate tag matches package version
        working-directory: packages/mcp
        run: |
          TAG_VERSION="${GITHUB_REF_NAME#mcp@}"
          PKG_VERSION=$(node -p "require('./package.json').version")
          if [ "$TAG_VERSION" != "$PKG_VERSION" ]; then
            echo "Tag version ($TAG_VERSION) does not match package.json version ($PKG_VERSION)"
            exit 1
          fi
      - run: bun run lint
        working-directory: packages/mcp
      - run: bun run typecheck
        working-directory: packages/mcp
      - run: bun run test
        working-directory: packages/mcp
      - run: bun run build
        working-directory: packages/mcp
      - uses: actions/upload-artifact@v5
        with:
          name: mcp-dist
          path: packages/mcp/dist

  publish-npm:
    needs: validate
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v5
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: ${{ env.BUN_VERSION }}
      - uses: actions/cache@v5
        with:
          path: ~/.bun/install/cache
          key: ${{ runner.os }}-bun-${{ hashFiles('**/bun.lock') }}
          restore-keys: ${{ runner.os }}-bun-
      - run: bun install --frozen-lockfile
      - uses: actions/download-artifact@v5
        with:
          name: mcp-dist
          path: packages/mcp/dist
      - uses: actions/setup-node@v5
        with:
          node-version: "22"
          registry-url: "https://registry.npmjs.org"
      - name: Resolve workspace dependencies for npm
        working-directory: packages/mcp
        run: |
          node <<'NODE'
          const fs = require("node:fs");

          const pkgPath = "package.json";
          const pkg = JSON.parse(fs.readFileSync(pkgPath, "utf8"));
          const ai = JSON.parse(fs.readFileSync("../sdk/js/ai/package.json", "utf8"));

          pkg.dependencies["@castai/ai-sdk"] = `^${ai.version}`;

          fs.writeFileSync(pkgPath, `${JSON.stringify(pkg, null, 2)}\n`);
          NODE
      - run: npm publish --provenance --access public
        working-directory: packages/mcp
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
